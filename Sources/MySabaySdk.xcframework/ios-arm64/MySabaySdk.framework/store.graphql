query GetProductsByServiceCode($serviceCode: String!) {
  store_listProduct(serviceCode: $serviceCode, pager:{page:1, limit:20}, orderBy: {salePrice: asc}) {
	products {
	  id
	  name
	  salePrice
	  currencyCode
	  isActive
	  properties
	}
  }
}
